name: Daily Scan KOSPI (KST 16:05)

on:
  schedule:
    - cron: "5 7 * * *"
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Seoul
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch prices
        run: python -m market_scanner.collectors.prices fetch --market kospi --worker 4

      - name: Compute indicators
        run: python -m market_scanner.analysis.indicators compute --market kospi

      - name: Run screener
        run: python -m market_scanner.analysis.screener run --market kospi
