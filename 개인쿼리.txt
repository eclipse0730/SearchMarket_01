select *
from markets m;

select *
from instruments i 
--where i.symbol  = 'CRCL'
--where name_en = 'Celestica'
where i.market_key = 'kosdaq'
order by instrument_id desc;

select market_key, asset_type, count(*)
from instruments i 
group by market_key, asset_type
order by market_key;

select *
from universe_memberships
order by created_at desc;

select um.universe_key, count(*)
from universe_memberships um 
group by universe_key;

select *
from universe_definitions
order by market_key desc, universe_key;


select *
from collection_runs
order by finished_at desc;


select * from daily_prices;



select name_en, name_local, count(*)
from daily_prices a
join instruments b on a.instrument_id = b.instrument_id 
group by name_en, name_local
order by count(*) asc

select * from daily_indicators;
select * from instrument_fundamentals;


SELECT symbol, market_key, asset_type, name_en
FROM instruments
WHERE symbol IN ('CLS', 'NVTS', 'CRCL', 'JOBY', 'OKLO')
AND market_key = 'us';










